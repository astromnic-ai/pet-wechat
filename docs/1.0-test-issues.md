# 1.0 测试记录问题清单

来源：`/Users/yangliu35/Downloads/1.0测试记录/1.0测试记录.md`

## 本次已直接修复

1. 手机端设备管理第三个入口不能再手动固件更新，需要改为重新配网。
   - 状态：本次已修复。
   - 修复：设备管理卡片第三个按钮改为“重新配网”，跳转搜索设备并进入重新配网模式；设置页移除“固件更新”卡片。
   - 代码：`packages/app/src/pages/devices/index.tsx`、`packages/app/src/pages/settings/index.tsx`

2. 重新配网入口和流程更新：主页离线后进入设备管理，点击重新配网，配网成功后直接回主页，不再进入选择宠物。
   - 状态：本次已修复。
   - 修复：新增 `mode=reconfigure` 流程，搜索设备和 WiFi 配置页识别重新配网模式；WiFi 成功后弹出“配网成功”弹窗，可返回主页或查看设备详情，不再进入选择宠物页。
   - 代码：`packages/app/src/pages/devices/index.tsx`、`packages/app/src/pages/collar-bind/index.tsx`、`packages/app/src/pages/wifi-config/index.tsx`

3. 设备联网页面需要提示“请选择 2.4G 网络进行配网”。
   - 状态：本次已修复。
   - 修复：WiFi 配置页在网络输入区域上方增加 2.4G 提示。
   - 代码：`packages/app/src/pages/wifi-config/index.tsx`、`packages/app/src/pages/wifi-config/index.scss`

4. 设备管理页只显示“桌面摆台”，不要显示后面的数字串。
   - 状态：本次已修复。
   - 修复：桌面设备卡片标题固定为“桌面摆台”，宠物绑定关系仍放在绑定标签里。
   - 代码：`packages/app/src/pages/devices/index.tsx`

5. 离线设备卡片白底灰字看不清楚。
   - 状态：本次已修复。
   - 修复：加深离线卡片的名称、状态、说明、标签文字颜色。
   - 代码：`packages/app/src/pages/devices/index.scss`

6. 等待页面进度图需要对应下方进度百分比，去掉等待“2-3 分钟”。
   - 状态：本次已修复。
   - 修复：定制进度页新增百分比圆环，文案改为“正在生成宠物形象/正在排队处理”，去除“预计需要 2-3 分钟”。
   - 代码：`packages/app/src/pages/avatar-progress/index.tsx`、`packages/app/src/pages/avatar-progress/index.scss`

7. 主页离线状态需要变更颜色，并显示上一次在线多久前。
   - 状态：本次已修复。
   - 修复：主页设备卡在线显示累计使用时长，离线显示“上次在线xx前”，离线状态胶囊改深灰。
   - 代码：`packages/app/src/pages/index/index.tsx`、`packages/app/src/pages/index/index.scss`、`packages/app/src/utils/deviceDisplay.ts`

8. 主页宠物图像偏小，希望兼容情况下再调大一点。
   - 状态：本次已修复。
   - 修复：放大主页宠物展示最大尺寸并略收紧舞台内边距。
   - 代码：`packages/app/src/pages/index/index.scss`

## 已确认代码已有对应处理

9. 设备二次绑定/已被其他账号绑定，最好在搜索设备页点击“连接”时直接提示，且提示中文、显示约 3 秒。
   - 状态：代码已有处理。
   - 依据：搜索设备页连接前调用 `/api/devices/ownership/check`，不可绑定时显示中文 toast，duration 为 3000ms；WiFi 注册阶段也会把英文占用错误归一为中文。
   - 代码：`packages/app/src/pages/collar-bind/index.tsx`、`packages/app/src/pages/wifi-config/index.tsx`

10. 重新上传主页图后手机端主页照片无法更新。
    - 状态：原记录已划掉，本次未再改。
    - 备注：如果复现，需要提供具体宠物、图片和缓存/接口返回。

11. 数据看板页面新增数据埋点，真实数据能否呈现需要评估。
    - 状态：代码已有真实数据页面和接口。
    - 依据：后台 `数据看板` 已通过 `/api/admin/analytics` 展示实时在线设备、平均互动数值、平均活跃数值、近 7 天用户累计活跃排行、今日在线宠物互动模式分布；数据来源包括设备在线时间、`interaction_events`、`pet_behaviors`、宠物活动模式。
    - 仍需确认：产品图里的“埋点”如果还有新增口径或字段，需要明确指标定义、采集事件名和展示位置。
    - 代码：`packages/admin/src/pages/Analytics.tsx`、`packages/server/src/routes/admin/analytics.ts`

## 原记录已标记修复，本次不重复处理

12. 加载页面图标替换。
13. 设备状态只显示一个开机动画。
14. 只有一个设备插电但显示了另一个未通电设备。
15. 等待图像定制时默认图像卡边、宠物形象位置不对。
16. 后台主页图上传提示词、格式、重新上传。
17. 小程序主页显示后台上传主页图。
18. 后台/小程序图像倒置。
19. 蓝牙绑定、联网、绑定宠物、设备图像过程节点没播到。
20. 系统日程保存后丢失、标签编辑清除。
21. 设置日程后设备未显示系统日程动作。
22. 后台行为配置“辛苦周五”改成“周末”。
23. 个性化日程默认当天无法取消。
24. 后台部分标签显示和睡觉标签颜色。
25. 图像上传后设备同步数量不对、反复下载失败。
26. 等待连接 WiFi 时显示蓝牙连接失败。
27. 连接 WiFi 后未选宠物就显示旧宠物形象。
28. WiFi 第一次连接失败，返回上一级后同密码才能连接。
29. 消息推送不明显、失败消息无法跳转重传、消息详情/红点。
30. 手机端宠物主页 PNG 底部被遮挡。
31. 同一用户添加新宠物并更换设备绑定后，设备下载未成功。
32. 设备下载进度回跳。
33. 宠物信息上方显示主页 PNG、性别图标对齐。
34. 一个宠物同时绑定两个设备时设备管理界面分不清设备。
35. 解除绑定后设备卡片“绑定宠物”标签不清晰。
36. 后台活跃设备只显示一台。
37. 设备更换网络环境后手机端无重新配网入口。
38. 离开网络环境显示动画不对应、无网络后显示蓝牙连接成功。
39. IMU 似乎没有反应、触摸待测试。

## 待澄清或需要设备端/真实数据验证

40. 设备显示号码无法分清楚。
    - 困惑点：当前记录没有明确期望是隐藏编号、显示短编号、还是按设备备注名区分；本次只处理了明确要求的“桌面摆台不带数字串”。

41. 设备持续插电时，后台和小程序离线/在线状态不稳定；手机端累计使用时长未更新。
    - 困惑点：需要真实设备心跳、设备 chipId、测试时间段和服务端数据才能判断是设备未上报、心跳超时阈值、还是前端显示问题。

42. 设备端下载进度比较慢，后台推送定制完成信息需要延迟，并等待设备端下载完成。
    - 困惑点：需要明确“推送完成”以哪个设备端下载状态为准，以及设备端是否有可靠的下载完成回传。

43. 设备端切换宠物，删除和下载原有宠物视频失败。
    - 困惑点：需要设备端日志、MQTT/接口交互记录和失败视频资源标识；本地小程序无法单独定位。

44. 小程序解绑设备当前宠物后，切换显示视频时有重叠未消失画面。
    - 困惑点：现象发生在设备端显示，需设备端渲染/播放状态机日志确认。

45. 设备状态几个图像比例小，定制图像占满屏幕，切换时会显示剪影。
    - 困惑点：记录写到“待统一画面尺寸再测试”，需要最终设备端画布尺寸和资源裁切规则。

46. 后台先删除设备动作、再删除 WiFi 信息、再删除后台设备后，设备动画没有对应变化，重新插拔后显示上一个宠物图像。
    - 困惑点：原记录标注“做了兼容逻辑，待设备端修改”，需要设备端离线/清缓存策略和 retained config 行为确认。

47. 6.25 页面偶尔显示左边图样、顶部栏乱码。
    - 困惑点：未说明具体页面路径和稳定复现步骤；需要复现视频或开发者工具日志。

48. 设备随机动作的播放限制。
    - 困惑点：没有说明限制规则，例如频率、时段、动作黑名单、还是播放冲突优先级；需要产品规则或设备端协议。
